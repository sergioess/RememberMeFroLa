import { PlantillaSubTarea } from './plantilla-sub-tarea';

describe('PlantillaSubTarea', () => {
  it('should create an instance', () => {
    expect(new PlantillaSubTarea()).toBeTruthy();
  });
});
